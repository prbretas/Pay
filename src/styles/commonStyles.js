import { StyleSheet} from 'react-native';

export const commonStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#5ff2f4',
    alignItems: 'center',
    justifyContent: 'center',
  },

  text:{
color:'green',
fontSize: 20,


  }
});
